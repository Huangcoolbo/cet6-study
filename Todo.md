# Todo

使用规则：
- 只记录仍然需要处理的任务
- 每条任务都要带状态
- 新发现的后续任务，也要补到这里
- 完成后把状态改掉，不要假装记住

## Status
- [ ] 未完成
- [~] 进行中
- [x] 已完成
- [-] 暂不处理

## Tasks
- [x] 为 `data/index/dingtalk-state.json` 补充字段级说明，解释每个字段的含义、用途、读写方与删除影响。
- [x] 新增贡献指南，说明其他账号如何提交 PR、哪些内容允许进入仓库、哪些内容不要提交。
- [x] 校正 `data/README.md`，补上 `input/` 的实际角色，并区分“当前已存在目录”与“规划中的资料目录”。
- [x] 修复 `sync-cet6-study.ps1` 在上游路径缺失时可能因 `Get-Item` 直接报错的问题，使缺失源路径按预期跳过。
- [x] 评估 `auto-push.ps1` 与 `sync-cet6-study.ps1` 的职责重叠，决定收敛为以 `sync-cet6-study.ps1` 为主入口，并把 `auto-push.ps1` 降级为兼容包装脚本，同时补充说明以避免后续维护混乱。
- [ ] 持续检查项目职责范围内是否还有未完成事项；如发现新的任务，补充到本文件。
- [ ] 评估是否要继续细化 `scripts/get-recommended-commit-title.ps1` / `sync-cet6-study.ps1` 对 `Todo.md + WORKFLOW.md + dingtalk-state.json` 这类高频维护组合的命名，避免它们长期稳定落成同一条合规但信息量偏低的 `chore: adjust CET-6 repository files ...` 标题。
- [x] 评估是否需要进一步收紧或细化自动生成的 `chore: adjust CET-6 repository files ...` / `sync: align CET-6 materials from D:\Bo` 标题模板，减少“虽然合规但重复度过高”的历史与 artifact 信息损耗。（结论：需要；已把 `sync-cet6-study.ps1` 的自动生成标题继续细化为优先按改动区域/对象输出，`data + automation` 混合改动会优先生成 `sync: refresh CET-6 data and automation for ...`，更杂的混合改动则优先汇总成 `index data`、`training inputs`、`study plans`、`project docs`、`automation` 等区域标签，减少同一条泛化 `sync:` / `chore:` 模板长期重复占满历史与 `title-audit-summary`）
- [~] 继续观察新的按区域自动生成标题是否已经足够减少重复标题分组；如果 `title-audit-summary` 里仍频繁出现高重复度合规标题，再决定是否继续细化到目录级或文件级对象命名。（本轮已根据最近 20 条历史抽查确认，重复最明显的一组仍是 `Todo.md + WORKFLOW.md + dingtalk-state.json` 反复落成同一条 `chore: adjust ...` 标题；已进一步收紧 `sync-cet6-study.ps1` 的混合改动命名：当主要涉及 `data/index/` 共享状态 + 少量配套文档时，优先生成 `data: refresh DingTalk reminder state and supporting docs` 或 `docs: clarify index data guidance and shared state files`，减少叶子文件枚举式重复标题；已把新标题补进 `scripts/test-validate-title.ps1`，当前本地 31 个样例通过。本轮又把同一套启发式抽到新的 `scripts/get-recommended-commit-title.ps1`，让自动同步之外的手动维护/巡检提交也能复用，并为 `Todo.md + WORKFLOW.md + dingtalk-state.json`、`Todo.md + WORKFLOW.md + *.ps1/.github` 这类高频组合补了更具体的 `review:` / `fix:` 标题候选，避免后续继续回落到 `chore: adjust CET-6 repository files ...`。这轮再次根据最新 20 条历史抽查发现，新出现的重复热点已经转成 `chore: adjust CET-6 repository areas index data, project docs, automation`；因此又继续收紧启发式：当 `dingtalk-state.json`、相关 workflow 文档与自动化脚本一起被调整时，优先改写为 `fix: refine DingTalk state workflow automation and docs` 或 `fix: refine DingTalk state workflow automation and follow-up tracking`，避免这类 `index data + project docs + automation` 维护继续退回到泛化区域汇总标题。后续继续观察真实新提交是否因此让高重复分组明显下降。此次又复跑最近 25 条历史与回归样例：`scripts/test-validate-title.ps1` 33 个样例通过，`audit-title-history.ps1 -Count 25 -AsJson` 显示 `Outcome=legacy-only`、`NeedsReview=false`、`UnknownFailureCount=0`，说明当前仍未出现新的明显误报/漏报；但顶部重复热点依然是 `chore: adjust CET-6 repository files Todo.md, WORKFLOW.md, dingtalk-state.json`（25 条里重复 6 次，且全是 PASS），说明“规则正确”与“标题信息量足够”仍不是一回事，后续应继续关注这类合规但过度重复的维护标题是否还会持续出现。本轮又根据最近 30 条历史审计确认，除 DingTalk 跟进组合外，`title-quality.yml + Todo.md + WORKFLOW.md` 与“同步兼容入口脚本 + backlog 文档”这两类组合也开始出现泛化倾向，因此继续把 `scripts/get-recommended-commit-title.ps1` 收紧为优先生成 `fix: refine title quality workflow guidance tests and backlog tracking`、`fix: refine sync entrypoint compatibility and workflow notes`；同时不再只识别最窄的三文件组合：若同批还连带改到 `COMMIT_MESSAGE_GUIDELINES.md`、`scripts/audit-title-history.ps1`、`scripts/get-recommended-commit-title.ps1`、`scripts/test-get-recommended-commit-title.ps1`、`scripts/test-validate-title.ps1` 这些标题质量支持文件，仍会保持这条更具体的 title-quality 标题，避免一多带相关测试/说明文件就退回 `fix: adjust repository automation ...`。也已把这类扩展组合补进 `scripts/test-get-recommended-commit-title.ps1` 回归样例，继续直接回归测试高频组合的建议标题生成，避免后续只测校验器、不测生成器。本轮再次复查最近 30 条历史后，又把两个此前未单独命名、但已经在历史里出现过的维护组合补进生成器：`Todo.md + data/index/dingtalk-state.json + maintenance-log.md` 现优先生成 `review: track DingTalk reminder follow-up and maintenance notes`，`.gitignore + Todo.md + WORKFLOW.md` 现优先生成 `chore: refine local audit artifact ignore rules and workflow notes`，继续减少这些巡检/收尾型提交退回到叶子文件枚举式 `chore: adjust ...` 的概率；对应回归样例也已补进 `scripts/test-get-recommended-commit-title.ps1`。本轮又继续针对最新 40 条历史里仍重复 3 次的 `fix: refine DingTalk state workflow automation and follow-up tracking` 做拆分：抽查实际提交后确认，它们大多是在 `Todo.md + WORKFLOW.md + dingtalk-state.json` 的基础上只连带标题审计/标题生成支持脚本，于是进一步把这类更窄的组合单独命名为 `review: refine DingTalk follow-up tracking and title automation`，避免“提醒跟进 + 标题规则收紧”继续挤进同一条较泛的 workflow automation 标题；同时也补认只出现叶子文件路径的 `title-quality.yml`，避免某些路径简写场景又退回通用文档标题。当前本地 `scripts/test-get-recommended-commit-title.ps1` 13 个样例、`scripts/test-validate-title.ps1` 33 个样例均已通过。最新这一轮继续抽查最近 50 条历史后，又发现 `title-quality.yml + Todo.md + WORKFLOW.md` 这条更具体的规则在“顺手同步 backlog / 巡检状态文件”时仍会回落：例如同批若再带上 `data/index/maintenance-log.md`、`data/index/task-board.md`，甚至 `data/index/dingtalk-state.json`，就会重新退回泛化 `chore: adjust ...`。因此这次又把这些 backlog / 跟进状态文件并入同一条 title-quality 维护规则，并补了对应回归样例，减少“标题质量工作流调优 + 巡检记录更新”继续刷成泛化标题的概率。后续继续观察这轮细化能否把最近历史里的重复热点进一步压散。本轮再按最近 60 条历史抽查后，除了已知历史坏标题桶外，仍看到两次旧式 `sync: align CET-6 materials from D:\Bo` 实际对应的是 `data/index/` 共享状态 / 队列文件 + `scripts/audit-title-history.ps1` 的组合，说明“index 跟进状态 + 标题审计脚本”这类巡检提交此前还会落到信息量偏低的同步标题。已继续把 `scripts/get-recommended-commit-title.ps1` 收紧：当只改 `data/index/` 文件并且脚本侧仅涉及标题审计 / 标题生成支持脚本时，现在优先命名为 `review: refine DingTalk index tracking and title audit automation`（若未碰 `dingtalk-state.json`，则为 `review: refine index tracking and title audit automation`），并补了两条基于真实历史文件组合的回归样例；当前 `scripts/test-get-recommended-commit-title.ps1` 已扩到 17 个样例并通过。后续继续观察这能否把“虽然合规、但看不出是索引巡检还是普通同步”的 `sync:` 标题进一步压下去。本轮又继续把路径匹配收紧为同时兼容全相对路径、反斜杠路径和仅叶子文件名的输入，避免标题生成器在 `title-quality.yml` / `maintenance-log.md` / `dingtalk-state.json` / 标题审计支持脚本等组合以简写路径出现时意外退回 `docs:` 或泛化 `chore:` 标题；并把这些叶子路径场景补进 `scripts/test-get-recommended-commit-title.ps1`，当前本地回归已扩到 19 个样例并通过。后续继续观察是否还有其他“同一语义、不同路径写法”导致的命名回退。最新这轮再根据最近 44 条历史的重复热点继续收紧：对于 `Todo.md + WORKFLOW.md + dingtalk-state.json` 再加 `data/index/` 跟进文件与标题审计/生成支持脚本的组合，不再只落到较宽的 `review: refine DingTalk follow-up tracking and title automation`，而会优先细化成 `review: refine DingTalk index follow-up guidance and title automation`；已把真实历史同型组合补进 `scripts/test-get-recommended-commit-title.ps1` 回归样例并通过，后续继续观察这能否把那组长期重复的 PASS 标题进一步压散。）
- [x] 评估是否要把 `scripts/get-recommended-commit-title.ps1` 进一步接入日常手动提交流程（例如维护脚本/提交流程说明），避免只有 `sync-cet6-study.ps1` 在使用更细的标题启发式。（结论：要，但先采用低风险的“提交流程说明接入”而不是再包一层新脚本；已在 `CONTRIBUTING.md`、`WORKFLOW.md`、`COMMIT_MESSAGE_GUIDELINES.md` 中补充手动提交时先 `git add`、再运行 `./scripts/get-recommended-commit-title.ps1`、必要时用 `./scripts/validate-title.ps1` 自检的推荐流程，让自动同步与人工维护共用同一套标题启发式。）
- [x] 制定更具体的 git commit message 规范，避免使用过于笼统的提交描述，并且不要包含时间戳。
- [x] 修复自动同步脚本仍在使用硬编码旧提交信息模板的问题，改为根据实际变更生成更具体、易懂的 commit message。
- [x] 评估是否需要把 `COMMIT_MESSAGE_GUIDELINES.md` 的约束进一步体现在自动化脚本或 PR 模板中，避免规范只停留在文档层。
- [x] 评估是否要进一步加入可执行的 commit / PR 标题校验（如本地 hook、GitHub Action 或审查脚本），把当前 PR 模板中的软约束升级为可检查规则。
- [~] 观察 `.github/workflows/title-quality.yml` 的实际误报/漏报情况，必要时调整标题校验规则，避免把合理提交拦得过死。（已补 `scripts/test-validate-title.ps1` 做首轮样本回归，并已放行正常 merge commit / 初始化历史标题；已根据真实 git 历史补放行 `Merge master into main` 这类正常合并标题；已把历史中的旧式大写 `Update ...` 标题加入失败回归样本，确认未来调规则时不会误放宽；已把回归样本接入 CI 工作流自身，并补了 `docs: README` / `fix: typo` / `chore: cleanup` 这类常见过短或空泛标题的失败样本；本轮又根据用户偏好收紧规则，新增“禁止日期/时间戳出现在 commit / PR 标题中”的校验与回归样例，并把回归脚本改成直接调用 `validate-title.ps1`，避免在当前环境里通过嵌套 `pwsh` 调用时偶发卡住；本次又根据真实仓库历史发现 `sync-cet6-study.ps1` 仍会生成不带 `<type>:` 前缀的旧式标题，因此已改为产出符合规则的 `data:` / `plan:` / `docs:` / `fix:` / `sync:` / `chore:` 标题，并把对应正反样例补进回归；又把真实历史中的 `update CET-6 study data and DingTalk reminder state; clarify project documentation; adjust repository automation` 这类“多类改动硬串在一个无前缀标题里”的旧样式补进失败回归，防止后续为放宽规则而误放行；本轮继续根据真实 git 历史补抓到 `chore: update CET-6 repository files ...` 这类仍会被校验器判定为“summary 以空泛 update 开头”的旧兜底样式，已把自动同步脚本兜底标题改成 `chore: adjust ...` 并在提交前先调用 `scripts/validate-title.ps1` 做自检，避免再次自动产出不合规标题；本轮又补了 `scripts/audit-title-history.ps1`，可直接批量审计最近真实 commit 历史并输出 PASS / FAIL + 原因，减少后续观察误报/漏报时只靠人工翻 log；这次继续把审计脚本增强为支持失败原因聚合摘要与 `-FailuresOnly` 只看失败项，并新增 `-SummaryOnly` / `-AsJson` 便于后续在巡检或 CI 中复用摘要结果；本地复跑 `scripts/test-validate-title.ps1` 29 个样例全部通过，并再次抽查最近 25 条历史：当前失败仍集中在已知历史遗留坏标题桶，最新一轮为 8 条带时间戳、3 条缺少 `<type>:` 前缀、2 条 `chore: update ...` 式空泛 summary，`audit-title-history.ps1 -SummaryOnly` 现会明确给出 `Outcome: legacy-only` / `Suggested action`，暂未看到新的明显误杀样例；后续继续结合真实 PR / push 记录观察）
- [x] 核对本机定时任务/恢复脚本是否仍在调用会产出旧式提交标题的历史版本，避免仓库继续被自动写入已被 CI 判定为不合规的 commit 标题。（已直接查询当前注册的 `CET6StudyAutoPush` / `CET6StudyResumeCatchup` 计划任务 XML，确认它们都指向 `D:\Ying` 下现行脚本：前者执行 `auto-push.ps1` 兼容包装层，后者执行 `resume-catchup.ps1` 且后续调用 `sync-cet6-study.ps1`；未发现仍指向旧目录或历史副本的已注册本机入口；并补了 `scripts/audit-sync-entrypoints.ps1` 便于后续重复核对）
- [x] 评估是否要把 `CET6StudyAutoPush` 计划任务的动作从 `auto-push.ps1` 兼容包装层进一步收敛为直接调用 `sync-cet6-study.ps1`，减少一层历史兼容入口并降低后续漂移风险。（结论：值得收敛；已补 `scripts/set-autopush-task-entrypoint.ps1` 作为可重复执行的迁移脚本，并更新 `scripts/audit-sync-entrypoints.ps1` / `WORKFLOW.md` 区分“首选主入口”与“兼容回退入口”）
- [x] 选择合适窗口执行 `scripts/set-autopush-task-entrypoint.ps1`，把本机 `CET6StudyAutoPush` 计划任务从兼容包装层正式切换为直接调用 `sync-cet6-study.ps1`，然后再次审计确认落地结果。（已执行迁移脚本并复跑 `scripts/audit-sync-entrypoints.ps1`，当前 `CET6StudyAutoPush` / `CET6StudyResumeCatchup` 均通过审计）
- [x] 清理计划任务审计过程中产生的本机 XML 快照边界，避免 `*.current.xml` / 导出任务定义被误当成仓库待提交内容。（已在 `.gitignore` 中忽略这类本机计划任务导出产物）
- [x] 评估是否要把 `scripts/audit-title-history.ps1 -AsJson` 接到后续巡检/CI 产物里，减少人工抄录标题审计摘要。（结论：值得接入；已把 push 范围的 JSON 审计结果接到 `.github/workflows/title-quality.yml`，并作为 `title-audit-summary` artifact 上传，便于后续下载复核）
- [~] 观察 `title-audit-summary` artifact 在真实 push 场景中的可读性与复用价值；如果后续巡检仍需要人工二次整理，再决定是否继续压缩摘要格式或补更多机器可消费字段。（已先把 `scripts/audit-title-history.ps1` 扩展为支持 `-AsMarkdown`，并让 `.github/workflows/title-quality.yml` 在 push 后同时产出 JSON + Markdown artifact，且把 Markdown 摘要直接写入 workflow step summary；本轮又补了 `-Compact` 模式，让 workflow summary 默认只显示统计 + 失败原因聚合，同时把完整逐条 Markdown 明细单独保留为 artifact，减少真实 push 时 summary 过长、还得人工扫完整列表的问题；本轮继续把“全绿”场景补得更直白：Markdown 摘要现在在 0 个失败时也会显式写出 `No failing titles found in this audit slice.`，并把失败原因计数里的 Unicode 乘号替换成 ASCII `x`，避免部分终端/日志编码下出现乱码；这次又把 JSON/控制台摘要往“可直接判断是否要继续追查”推进了一步：新增 `Outcome` 字段，并在每类失败原因下附少量示例标题，减少后续还得先翻完整明细才能判断是不是单纯命中历史遗留坏标题；本轮继续补了 `SuggestedAction` 提示，让 JSON / Markdown / 控制台摘要都能直接给出“无需跟进 / 仅需确认仍属历史坏标题 / 可能出现新规则问题”的下一步建议，进一步降低每次巡检先打开完整明细再判断的成本；本轮又根据真实历史抽查发现 `SuggestedAction` 对“仅命中已知历史坏标题桶”的识别过于脆弱，会把纯历史遗留问题误报成“存在新问题”，现已把失败原因归一化逻辑改成按已知模式稳定归类，并复跑最近 25 条历史确认摘要会正确提示“仍属已知 legacy bad-title buckets”；这轮又补了文档说明，明确 `Outcome` 现在分为 `clean` / `legacy-only` / `needs-review` 三种，避免后续看 workflow summary 或 artifact 时把“仅命中历史坏标题”误读成“完全无失败”或“出现新规则问题”；本轮继续把“这次到底审计了哪一段历史”也直接塞进摘要：JSON / Markdown / 控制台现都会输出 `AuditScope`（如 `latest 25 commits` 或具体 `revision range <before>..<after>`），减少后续看 artifact 时还得反推参数来源；本轮又补了更直接的机器消费信号：摘要现会额外输出 `NeedsReview` 布尔值与 `FailureBucketCount`，让后续巡检脚本或人工先看短摘要时就能判断“是否值得继续追”和“失败主要分成几类”；这轮继续把“审计范围边界”补到摘要里：JSON / Markdown / 控制台现在都会额外给出 `NewestCommit` / `OldestCommit` 的 SHA + 标题，减少后续拿到 artifact 还得再翻 git log 才能确认“这份摘要具体覆盖到哪两个端点”；本轮又补了 `RepeatedTitleCount` 与重复标题分组摘要，让巡检时即使范围内 0 个失败，也能一眼看出是否在反复使用同一条合规标题、导致历史与 workflow summary 信息量偏低；这次继续把“最值得先看的重复热点”直接推到短摘要：新增 `TopRepeatedTitleCount`、`TopRepeatedTitle` 与 `TopRepeatedTitleStatusMix`，让 workflow summary / JSON 摘要在不展开完整重复标题列表时，也能直接指出当前最严重的重复标题是哪一条、重复了几次、覆盖 PASS 还是 FAIL 样本，减少人工再翻 full details 的频率；本地已复跑 `scripts/test-validate-title.ps1` 并抽查 `audit-title-history.ps1 -Count 20 -AsJson` / `-AsMarkdown -Compact`，确认新增字段能和既有失败摘要、首尾 commit 边界、重复标题分组一起正常输出；后续重点观察这种“短摘要 + 审计范围 + 首尾边界 + 失败示例 + 重复标题分组 + 顶部重复热点 + 下一步建议 + 全量明细附件”的组合是否已经足够替代人工二次整理）
